---
trigger: always_on
---

Instructions for the browser_subagent:
1. Google Chrome is NOT installed on this system, and it will NEVER be installed. Do not attempt to open, search for, or install Google Chrome.
2. Under no circumstances should you mention Google Chrome or suggest installing it.
3. This system uses Safari. The browser automation tool `open_browser_url` manages browser contexts internally. If `open_browser_url` fails, report the error directly and ask the user how to proceed without reference to Google Chrome.
4. Do not waste tokens or steps attempting to run or troubleshoot Google Chrome.